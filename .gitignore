# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Game Boy ROMs (don't commit copyrighted content)
*.gb
*.gbc
*.sav

# GB Test ROMs - cloned during test run, not part of repo
gb-test-roms/

# Dependency directories (remove the comment below to include it)
# vendor/
roms/
.vscode/
.idea/

# Build artifacts
gomulator.exe
gomulator.wasm
gomulator-wasm.zip
wasm_exec.js
test_output*.txt
